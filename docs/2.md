# mongodb连接及增删改查
```js
import {MongoClient} from "mongodb"
 
// MongoDB 连接字符串
const url = 'mongodb://localhost:27017';
 
// 使用MongoClient来创建一个新的MongoDB连接
const client = new MongoClient(url);
 
// 连接服务器
async function connect() {
  try {
    await client.connect();
    console.log('连接成功');
  } catch (err) {
    console.error('连接失败: ', err);
  }
}

// 执行基本的CRUD操作

async function basicCRUDOperations() {
  try {
 
    // 获取数据库的引用
    const database = client.db('test');
    // console.log(database);
    // 获取集合的引用
    const collection = database.collection('test1');
 
    // 创建（Create）
    //await collection.insertOne({ a: "gugugaga" });
 
    // 读取（Read）
    //const findResult = await collection.findOne({ a: "gugugaga"});
    //console.log(findResult.a + findResult.a);
 
    // 更新（Update）
    //await collection.updateOne({ a: 1 }, { $set: { b: 1 } });
 
    // 删除（Delete）
    //await collection.deleteOne({ a: "gugugaga" });

    // 关闭连接
    await client.close();
  } catch (err) {
    console.error('执行基本 CRUD 操作时发生错误:', err);
  }
}
 
basicCRUDOperations();
 
connect();
```

